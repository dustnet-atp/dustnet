[package]
name = "content_particles"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[features]
default = ["materialise"]
materialise = []
atomise = []
prompt = []
lifecycle = []

[profile.release]
opt-level = "s"
lto = true
strip = true
