# dustnet-server

Static, plugin-free ATP server for
[Dustnet](https://github.com/roobert/dustnet).

Serves a directory over ATP with TLS 1.3 or loopback plaintext, with
compile-time connection bounds and no extension surface. See `dustnetd` for the
binary that wraps it.

Licensed under MIT OR Apache-2.0.
